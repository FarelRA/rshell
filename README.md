# P2P Remote Shell System

Peer-to-peer remote shell over UDP with NAT hole punching through a shared Go rendezvous server.

Implementations:
- Go
- Bun/Node.js
- Python
- PHP

See `docs/` for architecture, protocol, build, and interoperability details.
